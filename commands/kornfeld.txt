# Команды для YADRO Kornfeld OS (DC-коммутаторы D1156 / D2132)
# Версия UG: 1.7 (Ред. 7, 11.02.2026)
# Строки, начинающиеся с # — комментарии, игнорируются
# Пустые строки игнорируются
#
# ВАЖНО: '| no-more' добавляется автоматически драйвером KornfeldOSDriver
# ко всем show-командам — не нужно указывать вручную.

# --- Система и инвентаризация ---
show version
show system
show platform syseeprom

# --- Текущая конфигурация ---
show running-configuration

# --- Интерфейсы и L2 ---
show interface status
show interface
show ip interfaces
show PortChannel summary
show Vlan
show mac address-table
show spanning-tree

# --- IP, маршрутизация и соседи ---
show ip route
show ip arp
show ip vrf
show lldp neighbor

# --- Специфика ЦОД (Логическая топология фабрики) ---
show mclag brief
show bgp l2vpn evpn summary
show vxlan remote vtep
show bfd peers brief
show vrrp

# --- Логи ---
show logging lines 200
